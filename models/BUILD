# models/ — model metadata only (name, shape, weights identifier).
# Input preprocessing lives in inputs/. Runtime adapters live in runtimes/.

py_library(
    name = "loader",
    srcs = ["loader.py"],
    visibility = ["//visibility:public"],
)
