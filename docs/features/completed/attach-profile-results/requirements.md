# Goal

Let's have a column where we attach a text file showing the layer by layer profiling results. 

# Requirements

- by default, when we do benchmark run, we should generate layer by layer profiling results. 
- when we push to website, we should have a separate column that links the layer by layer file in a txt format that i can view right in the browser. 

ask clarifying questions if my understanding is off
- i am not sure where should these txt files should reside? 